{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently --names \"server,client\" \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  }
}
